<%- include('partials/header'); -%>

<h1>Home</h1>
<p><%= homeStartingContent %></p>

<div>
    <% posts.forEach(function(post){ %>
        <h1><%= post.name %> </h1>
        <p class="homeContent"><%= post.content %> </p>
        <a href="/posts/<%= post.name %>" >Read More...</a>
   <% }) %>
</div>

<%- include('partials/footer'); -%>